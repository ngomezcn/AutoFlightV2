#pragma once

#ifndef Simvars_h__
#define Simvars_h__

static enum DATA_DEFINE_ID {
	DEFINITION1,
};

static enum DATA_REQUEST_ID {
	REQUEST1,
};

struct SIMVARS {
	double THROTTLE1;
	double THROTTLE2;
};
#endif // Simvars_h__